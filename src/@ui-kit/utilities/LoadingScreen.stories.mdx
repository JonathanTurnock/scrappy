import { Stack } from "@fluentui/react"
import { Canvas, Meta, Story } from "@storybook/addon-docs/blocks"
import { LoadingScreen } from "./LoadingScreen"

<Meta title="UiKit/Utilities" component={LoadingScreen} />

# Loading Screen

Loading Screen with Centrally aligned Loading Spinner inside the Flex Container

<Canvas>
  <Story name="LoadingScreen">
    <Stack style={{ height: "500px" }}>
      <LoadingScreen />
    </Stack>
  </Story>
</Canvas>
